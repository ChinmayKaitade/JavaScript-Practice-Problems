// Beginner Level 🔥

// Higher-Order Functions

// Use `map()` to double all the elements in an array.

let arr = [1, 2, 3, 4, 5];

const result = arr.map((value) => {
  return value * 2;
});

console.log(result);
